# UniCORE.Caddy — Starting Point
Fork of upstream `caddyserver/caddy`. `main` tracks upstream (monthly upstream-merge). `unicore` = deploy branch (main + UniCORE additions: Badge/attribution, config, integration). Deploy ONLY from `unicore`. Created 2026-07-07 by Team UniCORE (uk-unicoreclaw-001-unicore).
